# Anthropic API key — required for Claude-powered generation
ANTHROPIC_API_KEY=sk-ant-...

# FastAPI backend URL — used by the Next.js API proxy route
BACKEND_URL=http://localhost:8000

# Postgres — used by FastAPI (matches docker-compose defaults)
DATABASE_URL=postgresql://rork:rork@localhost:5432/rork
